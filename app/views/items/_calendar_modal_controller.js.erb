angular.module('ItemModal', []).controller('CalendarModalController', function ($scope, $modalInstance, item) {
  console.log(item);
  $scope.item = item;

  $scope.ok = function () {
    $modalInstance.close(item);
  };

  $scope.cancel = function () {
    $modalInstance.dismiss('cancel');
  };
});